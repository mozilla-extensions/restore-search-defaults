{
  "name": "reset-search-defaults",
  "version": "1.0.0",
  "description": "Ask the user if they would like to use a specified search engine as the default.",
  "repository": "https://github.com/mozilla-extensions/restore-search-defaults",
  "license": "MPLv2",
  "private": true,
  "devDependencies": {
    "web-ext": "^5.4.1"
  },
  "scripts": {
    "build": "web-ext build -s extension --overwrite-dest --artifacts-dir . --filename reset-search-default.xpi",
    "lint": "web-ext lint -s extension"
  }
}
